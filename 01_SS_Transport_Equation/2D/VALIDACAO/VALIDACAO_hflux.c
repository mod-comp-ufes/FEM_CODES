#include "validacao.h"
#include <math.h>

double VALIDACAO_hflux(double X, double Y)
{
	double h;
	
	//h = 10*Y*(1-Y)*(exp(pow(X, 4.5)) - 2*exp(pow(X, 4.5))*X + 4.5*exp(pow(X, 4.5))*pow(X, 4.5) - 4.5*exp(pow(X, 4.5))*pow(X, 5.5));
	//h = 0;
	return h;

}
